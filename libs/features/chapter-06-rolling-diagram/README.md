# chapter-06-rolling-diagram

This library was generated with [Nx](https://nx.dev).

## Step numbering

The journey exposes four steps at `/chapter/6/step/{1..4}`. There is **no** `step-02-*.ts` definition file: URL step **2** is served by `step-03-cone.ts` (registry maps step number 2 → `STEP_03_CONE`). The archived `chapter-06-step-02` OpenSpec name reflects an early outline; the cone step was kept at file prefix `03` when an intermediate step was dropped. Routing, prerender, and `step-registry.ts` all agree on this mapping.
