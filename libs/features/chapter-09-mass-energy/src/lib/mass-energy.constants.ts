/** Worked-example speed for the rest-energy step (γ = 1.25). */
export const REST_ENERGY_V_OVER_C = 0.6;

/** Default photon energy (0–1, normalized) for the photon-in-a-box step. */
export const DEFAULT_PHOTON_ENERGY = 0.5;
