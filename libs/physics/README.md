# physics

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build physics` to build the library.

## Running unit tests

Run `nx test physics` to execute the unit tests via [Jest](https://jestjs.io).
